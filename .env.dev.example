# Configuration de la base de données PostgreSQL - Développement
# Note: Use "localhost" when running commands locally (migrations, seeds)
#       Use "postgres" when running inside Docker containers
POSTGRES_HOST=localhost
POSTGRES_DB=atelier_kaisla_dev
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432

# URL de l'API pour les applications frontend
# Note: All API routes are prefixed with /api (e.g., /api/products)
API_URL=http://localhost:4000/api

# Configuration Frontend (Nuxt)
# When running in Docker, use: http://backend:4000/api
# When running locally, use: http://localhost:4000/api
NUXT_PUBLIC_API_URL=http://backend:4000/api
